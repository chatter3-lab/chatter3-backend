{
  "name": "chatter3-backend",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:push": "wrangler d1 execute chatter3-db --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.8",
    "@cloudflare/workers-types": "^4.20241003.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
